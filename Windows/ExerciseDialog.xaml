<Window x:Class="Gymplanner.Windows.ExerciseDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Exercise" Height="300" Width="400" WindowStartupLocation="CenterOwner">

    <!-- Reusable style for pink, small buttons -->
    <Window.Resources>
        <Style x:Key="PinkSmallButton" TargetType="Button">
            <Setter Property="Width"      Value="60"/>
            <Setter Property="Height"     Value="30"/>
            <Setter Property="Margin"     Value="5"/>
            <Setter Property="Background" Value="Pink"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize"   Value="14"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Name:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="NameTextBox" Margin="5"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Description:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="DescriptionTextBox" Margin="5"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Muscle Group:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="MuscleGroupTextBox" Margin="5"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Difficulty:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="3" Grid.Column="1" x:Name="DifficultyComboBox" Margin="5">
            <ComboBoxItem Content="Beginner"/>
            <ComboBoxItem Content="Intermediate"/>
            <ComboBoxItem Content="Advanced"/>
        </ComboBox>

        <StackPanel Grid.Row="4" Grid.Column="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button x:Name="OkButton"
                    Content="OK"
                    Style="{StaticResource PinkSmallButton}"/>
            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Style="{StaticResource PinkSmallButton}"/>
        </StackPanel>
    </Grid>
</Window>

